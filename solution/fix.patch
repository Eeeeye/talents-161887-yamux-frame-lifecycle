--- a/const.go
+++ b/const.go
@@ -8,0 +9 @@
+	"os"
@@ -29,0 +31,12 @@
+// Unwrap exposes the underlying error for errors.Is and errors.As.
+func (e *NetError) Unwrap() error {
+	return e.err
+}
+
+// deadlineReached preserves the package's error text while exposing the
+// standard net.Conn deadline sentinel.
+type deadlineReached struct{}
+
+func (deadlineReached) Error() string { return "i/o deadline reached" }
+func (deadlineReached) Unwrap() error { return os.ErrDeadlineExceeded }
+
@@ -56 +69 @@
-		err: fmt.Errorf("i/o deadline reached"),
+		err: deadlineReached{},
@@ -60 +73,2 @@
-		timeout: true,
+		timeout:   true,
+		temporary: true,
--- a/stream.go
+++ b/stream.go
@@ -7,0 +8 @@
+	"errors"
@@ -111 +112 @@
-		fallthrough
+		// LocalClose only prohibits further local writes. Handle reads normally.
@@ -140,0 +142,3 @@
+	if errors.Is(err, ErrSessionShutdown) || errors.Is(err, io.ErrClosedPipe) {
+		err = nil
+	}
@@ -218,0 +223,4 @@
+		if errors.Is(err, ErrSessionShutdown) || errors.Is(err, ErrConnectionWriteTimeout) {
+			// Message left in ready queue, header re-use is unsafe.
+			s.sendHdr = header(make([]byte, headerSize))
+		}
@@ -298,0 +307,4 @@
+		if errors.Is(err, ErrSessionShutdown) || errors.Is(err, ErrConnectionWriteTimeout) {
+			// Message left in ready queue, header re-use is unsafe.
+			s.controlHdr = header(make([]byte, headerSize))
+		}
@@ -312,0 +325,4 @@
+		if errors.Is(err, ErrSessionShutdown) || errors.Is(err, ErrConnectionWriteTimeout) {
+			// Message left in ready queue, header re-use is unsafe.
+			s.controlHdr = header(make([]byte, headerSize))
+		}
@@ -501 +517,2 @@
-	if _, err := io.Copy(s.recvBuf, conn); err != nil {
+	copiedLength, err := io.Copy(s.recvBuf, conn)
+	if err != nil {
@@ -508 +525 @@
-	s.recvWindow -= length
+	s.recvWindow -= uint32(copiedLength)
@@ -529,0 +547 @@
+	asyncNotify(s.recvNotifyCh)
@@ -535,0 +554 @@
+	asyncNotify(s.sendNotifyCh)
--- a/session.go
+++ b/session.go
@@ -193,0 +194,4 @@
+
+	if s.config.StreamOpenTimeout > 0 {
+		go s.setOpenTimeout(stream)
+	}
@@ -269 +272,0 @@
-// Attempts to send a GoAway before closing the connection.
@@ -270,0 +274,14 @@
+	if !s.startShutdown(ErrSessionShutdown) {
+		return nil
+	}
+
+	_ = s.conn.Close()
+	<-s.recvDoneCh
+	s.forceCloseStreams()
+	<-s.sendDoneCh
+	return nil
+}
+
+// startShutdown marks the session as shutting down and closes shutdownCh
+// once. The caller that wins this race completes the rest of shutdown.
+func (s *Session) startShutdown(err error) bool {
@@ -275 +292 @@
-		return nil
+		return false
@@ -281 +298 @@
-		s.shutdownErr = ErrSessionShutdown
+		s.shutdownErr = err
@@ -286,3 +303,2 @@
-
-	_ = s.conn.Close()
-	<-s.recvDoneCh
+	return true
+}
@@ -289,0 +306,2 @@
+// forceCloseStreams closes every registered stream during session shutdown.
+func (s *Session) forceCloseStreams() {
@@ -295 +312,0 @@
-	return nil
@@ -299 +316 @@
-// session to terminate.
+// session to terminate. It must not re-enter blocking Close.
@@ -301,3 +318,2 @@
-	s.shutdownErrLock.Lock()
-	if s.shutdownErr == nil {
-		s.shutdownErr = err
+	if !s.startShutdown(err) {
+		return
@@ -305,2 +321,2 @@
-	s.shutdownErrLock.Unlock()
-	_ = s.Close()
+	_ = s.conn.Close()
+	s.forceCloseStreams()
@@ -338,0 +355 @@
+		s.retirePing(id, ch)
@@ -347,3 +364 @@
-		s.pingLock.Lock()
-		delete(s.pings, id) // Ignore it if a response comes later.
-		s.pingLock.Unlock()
+		s.retirePing(id, ch)
@@ -351,0 +367 @@
+		s.retirePing(id, ch)
@@ -358,0 +375,8 @@
+func (s *Session) retirePing(id uint32, ch chan struct{}) {
+	s.pingLock.Lock()
+	if s.pings[id] == ch {
+		delete(s.pings, id)
+	}
+	s.pingLock.Unlock()
+}
+
@@ -409,0 +434,20 @@
+	bodyCopy := func() {
+		if body == nil {
+			return // A nil body is ignored.
+		}
+
+		// In the event of session shutdown or connection write timeout,
+		// we need to prevent `send` from reading the body buffer after
+		// returning from this function since the caller may re-use the
+		// underlying array.
+		ready.mu.Lock()
+		defer ready.mu.Unlock()
+
+		if ready.Body == nil {
+			return // Body was already copied in `send`.
+		}
+		newBody := make([]byte, len(body))
+		copy(newBody, body)
+		ready.Body = newBody
+	}
+
@@ -413,0 +458 @@
+		bodyCopy()
@@ -415,0 +461 @@
+		bodyCopy()
@@ -609 +655,2 @@
-	// Check if this is a query, respond back.
+	// Check if this is a query, respond back in a separate context so we
+	// don't interfere with the receiving thread blocking for the write.
@@ -611,3 +658,8 @@
-		hdr := header(make([]byte, headerSize))
-		hdr.encode(typePing, flagACK, 0, pingID)
-		return s.sendNoWait(hdr)
+		go func() {
+			hdr := header(make([]byte, headerSize))
+			hdr.encode(typePing, flagACK, 0, pingID)
+			if err := s.sendNoWait(hdr); err != nil {
+				s.logger.Printf("[WARN] yamux: failed to send ping reply: %v", err)
+			}
+		}()
+		return nil
